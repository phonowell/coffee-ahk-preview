import replaceConfig_ from './replaceConfig_'
import replaceIfNotAwait_ from './replaceIfNotAwait_'
import replace_ from './replace_'

// export
export default {
  replaceConfig_,
  replaceIfNotAwait_,
  replace_,
}
